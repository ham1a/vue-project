pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/ham1a/vue-project'
            }
        }

        stage('Build') {
            steps {
                echo 'Build commands here'
            }
        }
    }
}
